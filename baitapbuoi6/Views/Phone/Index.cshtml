@model List<baitapbuoi6.Models.Phone>
@{
    ViewData["Title"] = "Danh sách điện thoại";
}

<h2>Danh sách điện thoại</h2>
<a asp-action="Create" class="btn btn-primary mb-3">Thêm mới</a>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>ID</th>
            <th>Tên máy</th>
            <th>Hãng</th>
            <th>Giá ($)</th>
            <th>Số lượng</th>
            <th>Thao tác</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.ModelName</td>
                <td>@item.Brand</td>
                <td>@item.Price</td>
                <td>@item.Stock</td>
                <td>
                    <a asp-action="Detail" asp-route-id="@item.Id" class="btn btn-info btn-sm">Chi tiết</a>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Sửa</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm" onclick="return confirm('Bạn có chắc chắn muốn xóa @item.ModelName không?');">Xóa</a>
                </td>
            </tr>
        }
    </tbody>
</table>